{% extends 'base.html.twig' %}

{% block title %}Categories – Eco Meal{% endblock %}

{% block body %}
    <div class="eco-page-header">
        <h1>Categories</h1>
        <a href="{{ path('app_category_new') }}" class="eco-btn eco-btn-primary">+ New Category</a>
    </div>

    <div class="eco-table-wrap">
        <div style="overflow-x:auto;">
            <table class="eco-table">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for category in categories %}
                    <tr>
                        <td class="id-cell">{{ category.id }}</td>
                        <td>
                            <a href="{{ path('app_category_view', {id: category.id}) }}" class="table-link">
                                {{ category.name }}
                            </a>
                        </td>
                        <td>
                            <div style="display: flex; gap: 6px;">
                                <a href="{{ path('app_category_edit', {id: category.id}) }}" class="eco-btn eco-btn-ghost" style="padding: 6px 14px; font-size: 13px;">Edit</a>
                                <a href="{{ path('app_category_delete', {id: category.id}) }}" class="eco-btn eco-btn-danger" style="padding: 6px 14px; font-size: 13px;" onclick="return confirm('Delete this category?')">Delete</a>
                            </div>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="3">
                            <div class="eco-empty">No categories found.</div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
