{% extends 'base.html.twig' %}

{% block title %}Edit Category – Eco Meal{% endblock %}

{% block body %}
    <div class="eco-page-header">
        <h1>Edit Category</h1>
        <a href="{{ path('app_category_view', {id: form.vars.data.id}) }}" class="eco-btn eco-btn-ghost">← Back</a>
    </div>

    <div class="eco-form-card">
        <p class="eco-form-title">{{ form.vars.data.name }}</p>
        {{ form_start(form) }}
        {{ form_row(form.name) }}

        <div style="display: flex; gap: 10px; margin-top: 24px;">
            {{ form_widget(form.submit, {
                attr: { class: 'eco-btn eco-btn-primary', style: 'flex: 1; margin: 0; border: none; cursor: pointer;' },
                label: 'Save Changes'
            }) }}
            <a href="{{ path('app_category_view', {id: form.vars.data.id}) }}" class="eco-btn eco-btn-cancel" style="flex: 1; text-align: center; display: inline-flex; justify-content: center; align-items: center;">Cancel</a>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
