{% extends 'base.html.twig' %}

{% block title %}New Consumer – Eco Meal{% endblock %}

{% block body %}
    <div class="eco-page-header">
        <h1>New Consumer</h1>
        <a href="{{ path('app_consumer') }}" class="eco-btn eco-btn-ghost">← Back</a>
    </div>

    <div class="eco-form-card">
        <p class="eco-form-title">Consumer Details</p>
        {{ form_start(form) }}
        {{ form_row(form.firstName) }}
        {{ form_row(form.lastName) }}
        {{ form_row(form.phoneNumber) }}

        {% if form.email is defined %}
            <hr class="my-4" style="border-top: 1px solid var(--border); margin: 24px 0;">
            <p class="eco-form-title">Account Credentials</p>
            {{ form_row(form.email) }}
            {{ form_row(form.password) }}
        {% endif %}

        <div style="display: flex; gap: 10px; margin-top: 24px;">
            {{ form_widget(form.submit, {
                attr: { class: 'eco-btn eco-btn-primary', style: 'flex: 1; margin: 0; border: none; cursor: pointer;' },
                label: 'Create Consumer'
            }) }}
            <a href="{{ path('app_consumer') }}" class="eco-btn eco-btn-cancel" style="flex: 1; text-align: center; display: inline-flex; justify-content: center; align-items: center;">Cancel</a>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
